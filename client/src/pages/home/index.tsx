import HomePanel from './HomePanel';
import Layout from '../../components/Layout';

const HomePage = () => {
  return (
    <Layout>
      <HomePanel />
    </Layout>
  );
};

export default HomePage;
